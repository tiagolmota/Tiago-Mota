# MarkBridge — Desktop App Dependencies
# Install: pip install -r scripts/markitdown-app/requirements.txt

# Core conversion engine
markitdown[pdf,docx,pptx,xlsx,youtube-transcription]>=0.1.0

# GUI
customtkinter>=5.2.0

# System tray + icon
pystray>=0.19.0
pillow>=10.0.0

# Watch folder
watchdog>=4.0.0

# Standalone executable (optional — build only)
# pyinstaller>=6.0.0
